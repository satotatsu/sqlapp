CREATE FUNCTION FUNC (AAA CHAR(3))
    RETURNS TABLE (EMPNO CHAR(6),
                    LASTNAME VARCHAR(15),
                    FIRSTNAME VARCHAR(12)
                    , POS DECIMAL(10,6)
                    , NUM INT)
     LANGUAGE SQL
     MODIFIES SQL DATA
     NO EXTERNAL ACTION
     DETERMINISTIC
     BEGIN ATOMIC
       INSERT INTO AUDIT 
       VALUES (USER, 
               'Table: EMPLOYEE Prd: DEPTNO = ' CONCAT DEPTNO);
   RETURN
     SELECT EMPNO, LASTNAME, FIRSTNME
       FROM EMPLOYEE
       WHERE EMPLOYEE.WORKDEPT = DEPTEMPLOYEES.DEPTNO
END