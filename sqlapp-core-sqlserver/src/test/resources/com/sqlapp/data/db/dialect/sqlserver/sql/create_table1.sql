CREATE TABLE tableA
(
	  colA INT NOT NULL
	, colB BIGINT NOT NULL CHECK (colB>0)
	, colC VARCHAR(10) DEFAULT '0' NOT NULL
	, colD DATETIME2(7) NOT NULL
	, colE DATETIMEOFFSET(7) NOT NULL
	, CONSTRAINT PK_TABLEA PRIMARY KEY ( colA, colB )
	, CONSTRAINT UK_tableA1 UNIQUE ( colB )
	, CONSTRAINT FK1 FOREIGN KEY ( colA ) REFERENCES tableB ( colA ) ON DELETE CASCADE ON UPDATE RESTRICT
)