SELECT
  A.OBJECT_NAME
, A.ANNOTATION_OWNER
, A.ANNOTATION_NAME
, A.ANNOTATION_VALUE
FROM ALL_ANNOTATIONS_USAGE A
WHERE A.OBJECT_TYPE='DOMAIN'
  AND A.COLUMN_NAME IS NULL
  /*if isNotEmpty(schemaName)*/
  AND A.ANNOTATION_OWNER IN /*schemaName*/('%')
  /*end*/
  /*if isNotEmpty(domainName)*/
  AND A.OBJECT_NAME IN /*domainName*/('%')
  /*end*/
ORDER BY A.ANNOTATION_OWNER, A.OBJECT_NAME, A.ANNOTATION_NAME
