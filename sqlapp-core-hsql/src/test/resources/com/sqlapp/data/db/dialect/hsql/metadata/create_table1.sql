CREATE TABLE TAB1
(
	  ID INTEGER GENERATED BY DEFAULT AS IDENTITY ( START WITH 1 )
	, CODE        char(5)
	, TITLE      varchar(40)
	, DATETIME_COL DATETIME(4) NOT NULL
	, amount DECIMAL(15,2) NOT NULL
	, CONSTRAINT PK_TAB1 PRIMARY KEY (ID)
)
